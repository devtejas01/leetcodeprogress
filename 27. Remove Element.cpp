class Solution {
    public:
        int removeElement(vector<int>& nums, int val) { int right=nums.size()-1;
        int left=0;
        while(left<right)
        {
            if(nums[right]==target)
            {
                nums[right]=-1;
                
            }
           if(nums[left]==target)
           {
            swap(num[])
    
           }
           right--
        }
            
        }
    };